# Slim dependency set for the DEMO_MODE deployment (Streamlit Community Cloud).
#
# app.py never imports agent.email_handler when DEMO_MODE=true, so the demo
# never touches chromadb / sentence-transformers / torch / langchain_* /
# google-genai / openai — none of that needs to be installed for the demo to
# work. requirements.txt (full) remains the source of truth for local dev
# and any deployment that runs the real pipeline.
streamlit==1.57.0
pandas==3.0.3
altair==6.1.0
python-dotenv==1.2.2
